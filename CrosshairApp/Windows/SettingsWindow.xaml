<Window x:Class="CrosshairApp.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Crosshair Settings"
        Height="800" Width="420"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        Background="#1e1e1e">

	<Window.Resources>
		<BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
		<SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="#2d2d30" />
		<SolidColorBrush x:Key="TertiaryBackgroundBrush" Color="#37373A" />
		<SolidColorBrush x:Key="ControlBackgroundBrush" Color="#3c3c3c" />
		<SolidColorBrush x:Key="BorderBrush" Color="#4a4a4a" />
		<SolidColorBrush x:Key="PrimaryForegroundBrush" Color="#dcdcdc" />
		<SolidColorBrush x:Key="SecondaryForegroundBrush" Color="#808080" />
		<SolidColorBrush x:Key="AccentBrush" Color="#007acc" />
		<SolidColorBrush x:Key="AccentHoverBrush" Color="#0099ff" />
		<SolidColorBrush x:Key="AccentPressedBrush" Color="#005c99" />
		<SolidColorBrush x:Key="FooterBackgroundBrush" Color="#252526" />

		<Style x:Key="ScrollBarThumbStyle" TargetType="{x:Type Thumb}">
			<Setter Property="SnapsToDevicePixels" Value="True" />
			<Setter Property="OverridesDefaultStyle" Value="true" />
			<Setter Property="IsTabStop" Value="false" />
			<Setter Property="Focusable" Value="false" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Thumb}">
						<Border CornerRadius="4" Background="{StaticResource AccentPressedBrush}"
                                BorderBrush="Transparent" BorderThickness="1" />
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style x:Key="ScrollBarPageButtonStyle" TargetType="{x:Type RepeatButton}">
			<Setter Property="SnapsToDevicePixels" Value="True" />
			<Setter Property="OverridesDefaultStyle" Value="true" />
			<Setter Property="IsTabStop" Value="false" />
			<Setter Property="Focusable" Value="false" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type RepeatButton}">
						<Border Background="Transparent" />
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="{x:Type ScrollBar}">
			<Setter Property="SnapsToDevicePixels" Value="True" />
			<Setter Property="OverridesDefaultStyle" Value="true" />
			<Setter Property="Stylus.IsFlicksEnabled" Value="false" />
			<Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
			<Setter Property="Background" Value="Transparent" />
			<Setter Property="Width" Value="10" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type ScrollBar}">
						<Grid x:Name="GridRoot" Width="{TemplateBinding Width}"
                              Background="{TemplateBinding Background}">
							<Track x:Name="PART_Track" IsDirectionReversed="true" Focusable="false">
								<Track.DecreaseRepeatButton>
									<RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}"
                                                  Command="ScrollBar.PageUpCommand" />
								</Track.DecreaseRepeatButton>
								<Track.Thumb>
									<Thumb Style="{StaticResource ScrollBarThumbStyle}" Margin="-2,5,5,5" />
								</Track.Thumb>
								<Track.IncreaseRepeatButton>
									<RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}"
                                                  Command="ScrollBar.PageDownCommand" />
								</Track.IncreaseRepeatButton>
							</Track>
						</Grid>
						<ControlTemplate.Triggers>
							<Trigger SourceName="PART_Track" Property="IsMouseOver" Value="true">
								<Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}" />
							</Trigger>
							<Trigger SourceName="PART_Track" Property="IsMouseOver" Value="false">
								<Setter Property="Background" Value="Transparent" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="CheckBox">
			<Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}" />
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="Margin" Value="0,10,0,5" />
			<Setter Property="Padding" Value="8,0,0,0" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="CheckBox">
						<Grid x:Name="templateRoot" Background="Transparent" SnapsToDevicePixels="True">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto"/>
								<ColumnDefinition Width="*"/>
							</Grid.ColumnDefinitions>

							<Border x:Name="checkBoxBorder"
                                    Grid.Column="0"
                                    Width="20"
                                    Height="20"
                                    Background="{StaticResource ControlBackgroundBrush}"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="4">
								<Viewbox Width="12" Height="12">
									<Path x:Name="checkMark"
                                          Data="M6,11 L10,15 L18,7"
                                          Stroke="White"
                                          StrokeThickness="2"
                                          Visibility="Collapsed"/>
								</Viewbox>
							</Border>
							<ContentPresenter Grid.Column="1"
                                              Content="{TemplateBinding Content}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
						</Grid>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
							</Trigger>
							<Trigger Property="IsPressed" Value="True">
								<Setter TargetName="checkBoxBorder" Property="Background" Value="{StaticResource AccentPressedBrush}" />
								<Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{StaticResource AccentPressedBrush}" />
							</Trigger>
							<Trigger Property="IsChecked" Value="True">
								<Setter TargetName="checkMark" Property="Visibility" Value="Visible" />
								<Setter TargetName="checkBoxBorder" Property="Background" Value="{StaticResource AccentBrush}" />
								<Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
							</Trigger>
							<Trigger Property="IsEnabled" Value="False">
								<Setter Property="Opacity" Value="0.5" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
		<Style TargetType="Label">
			<Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}" />
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="Margin" Value="0,10,0,5" />
		</Style>
		<Style TargetType="TextBlock">
			<Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}" />
			<Setter Property="VerticalAlignment" Value="Center" />
		</Style>

		<Style x:Key="PrimaryButtonStyle" TargetType="Button">
			<Setter Property="Background" Value="{StaticResource AccentBrush}" />
			<Setter Property="Foreground" Value="White" />
			<Setter Property="BorderThickness" Value="0" />
			<Setter Property="Cursor" Value="Hand" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border Background="{TemplateBinding Background}" CornerRadius="7" Padding="10, 5">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter Property="Background" Value="{StaticResource AccentHoverBrush}" />
							</Trigger>
							<Trigger Property="IsPressed" Value="True">
								<Setter Property="Background" Value="{StaticResource AccentPressedBrush}" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
		<Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}" />

		<Style x:Key="InputControlStyle" TargetType="{x:Type Control}">
			<Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}" />
			<Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}" />
			<Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
			<Setter Property="BorderThickness" Value="1" />
			<Setter Property="Padding" Value="8" />
			<Setter Property="VerticalContentAlignment" Value="Center" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Control}">
						<Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4"
                                SnapsToDevicePixels="True">
							<ScrollViewer x:Name="PART_ContentHost" Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="hidden" />
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter Property="BorderBrush" TargetName="border" Value="{StaticResource AccentBrush}" />
							</Trigger>
							<Trigger Property="IsKeyboardFocused" Value="True">
								<Setter Property="BorderBrush" TargetName="border" Value="{StaticResource AccentBrush}" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
		<Style TargetType="TextBox" BasedOn="{StaticResource InputControlStyle}" />

		<Style x:Key="ModernComboBoxStyle" TargetType="{x:Type ComboBox}">
			<Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}" />
			<Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}" />
			<Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
			<Setter Property="BorderThickness" Value="1" />
			<Setter Property="Padding" Value="8" />
			<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
			<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type ComboBox}">
						<Grid x:Name="templateRoot" SnapsToDevicePixels="true">
							<Popup x:Name="PART_Popup" AllowsTransparency="true"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}"
                                   Placement="Bottom">
								<Border x:Name="dropDownBorder" Background="{StaticResource SecondaryBackgroundBrush}"
                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
									<ScrollViewer x:Name="DropDownScrollViewer">
										<ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
									</ScrollViewer>
								</Border>
							</Popup>
							<ToggleButton x:Name="toggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
								<ToggleButton.Template>
									<ControlTemplate TargetType="ToggleButton">
										<Border x:Name="templateBorder" Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" />
									</ControlTemplate>
								</ToggleButton.Template>
							</ToggleButton>
							<ContentPresenter ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              IsHitTestVisible="false"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
						</Grid>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
							</Trigger>
							<Trigger Property="IsKeyboardFocused" Value="True">
								<Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
		<Style TargetType="ComboBox" BasedOn="{StaticResource ModernComboBoxStyle}" />

		<Style TargetType="{x:Type Slider}">
			<Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Slider}">
						<Grid>
							<Track x:Name="PART_Track">
								<Track.DecreaseRepeatButton>
									<RepeatButton Command="{x:Static Slider.DecreaseLarge}">
										<RepeatButton.Style>
											<Style TargetType="RepeatButton">
												<Setter Property="OverridesDefaultStyle" Value="true" />
												<Setter Property="IsTabStop" Value="false" />
												<Setter Property="Focusable" Value="false" />
												<Setter Property="Template">
													<Setter.Value>
														<ControlTemplate TargetType="RepeatButton">
															<Border Height="4" CornerRadius="2"
                                                                    Background="{TemplateBinding Foreground}"
                                                                    Margin="0,0,-1,0" />
														</ControlTemplate>
													</Setter.Value>
												</Setter>
											</Style>
										</RepeatButton.Style>
									</RepeatButton>
								</Track.DecreaseRepeatButton>
								<Track.IncreaseRepeatButton>
									<RepeatButton Command="{x:Static Slider.IncreaseLarge}">
										<RepeatButton.Style>
											<Style TargetType="RepeatButton">
												<Setter Property="OverridesDefaultStyle" Value="true" />
												<Setter Property="IsTabStop" Value="false" />
												<Setter Property="Focusable" Value="false" />
												<Setter Property="Template">
													<Setter.Value>
														<ControlTemplate TargetType="RepeatButton">
															<Border Height="4" CornerRadius="2"
                                                                    Background="{StaticResource ControlBackgroundBrush}" />
														</ControlTemplate>
													</Setter.Value>
												</Setter>
											</Style>
										</RepeatButton.Style>
									</RepeatButton>
								</Track.IncreaseRepeatButton>
								<Track.Thumb>
									<Thumb>
										<Thumb.Style>
											<Style TargetType="Thumb">
												<Setter Property="OverridesDefaultStyle" Value="True" />
												<Setter Property="Template">
													<Setter.Value>
														<ControlTemplate TargetType="Thumb">
															<Ellipse Fill="{TemplateBinding Foreground}" Height="16"
                                                                     Width="16" />
														</ControlTemplate>
													</Setter.Value>
												</Setter>
											</Style>
										</Thumb.Style>
									</Thumb>
								</Track.Thumb>
							</Track>
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="GroupBox">
			<Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
			<Setter Property="BorderThickness" Value="1" />
			<Setter Property="Padding" Value="10,5" />
			<Setter Property="Margin" Value="0,0,0,10" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="GroupBox">
						<Grid>
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="*" />
							</Grid.RowDefinitions>
							<Border Grid.Row="0" Background="{StaticResource TertiaryBackgroundBrush}"
                                    CornerRadius="4,4,0,0" BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="0,0,0,1">
								<Label Content="{TemplateBinding Header}"
                                       Foreground="{StaticResource PrimaryForegroundBrush}" FontWeight="Bold"
                                       Padding="10,1" />
							</Border>
							<Border Grid.Row="1" Background="{StaticResource SecondaryBackgroundBrush}"
                                    CornerRadius="0,0,4,4">
								<ContentPresenter Margin="{TemplateBinding Padding}" />
							</Border>
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</Window.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<Border Grid.Row="0" Background="{StaticResource AccentBrush}" Padding="10,5">
			<TextBlock Text="Crosshair Customization" FontSize="20" FontWeight="SemiBold" Foreground="White"
                       HorizontalAlignment="Center" />
		</Border>

		<ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      Padding="20,10,20,15">
			<StackPanel>

				<GroupBox Header="ADS Settings">
					<StackPanel>
						<CheckBox x:Name="EnableAdsProfileCheckBox" Content="Enable different crosshair for ADS" Margin="0,10,0,5" Checked="OnAdsSettingChanged" Unchecked="OnAdsSettingChanged" />
						<ComboBox x:Name="AdsProfileComboBox" Margin="0,5,0,0" SelectionChanged="OnAdsProfileComboBoxSelectionChanged" Visibility="{Binding IsChecked, ElementName=EnableAdsProfileCheckBox, Converter={StaticResource BooleanToVisibilityConverter}}"/>
					</StackPanel>
				</GroupBox>

				<GroupBox Header="Profiles">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*" />
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="Auto" />
						</Grid.ColumnDefinitions>
						<ComboBox x:Name="ProfileComboBox" Grid.Column="0" Margin="0,0,10,0"
                                  SelectionChanged="ProfileComboBox_SelectionChanged" />

						<Button Content="Add" Grid.Column="1" Margin="0,0,5,0"
                                Click="AddProfileButton_Click" VerticalAlignment="Center" />
						<Button Content="Delete" Grid.Column="2" Margin="0,0,5,0"
                                Click="DeleteProfileButton_Click" VerticalAlignment="Center" />

						<Button x:Name="ResetProfileButton" Content="Reset to Default" Margin="0,0,5,0" Grid.Column="3"
                                Click="ResetProfileButton_Click" Visibility="Collapsed" VerticalAlignment="Center" />
					</Grid>
				</GroupBox>

				<GroupBox Header="General">
					<StackPanel>
						<!-- Dynamic Color Checkbox -->
						<CheckBox x:Name="DynamicColorCheckBox" Content="Enable Dynamic Smart Color"
                                  Margin="0,5,0,10" Checked="OnDynamicColorChanged" Unchecked="OnDynamicColorChanged" />

						<Label Content="Style:" />
						<ComboBox x:Name="StyleComboBox" SelectionChanged="OnSettingChanged" />

						<Label Content="Color (HEX/RGB):" />
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>
							<TextBox x:Name="ColorTextBox" Grid.Column="0" TextChanged="OnSettingChanged" />
							<Button x:Name="PickColorButton" Grid.Column="1" Content="Pick Color" Margin="5,0,0,0" Click="PickColorButton_Click"
									VerticalAlignment="Center" />
						</Grid>

						<Label Content="Outline Color (HEX/RGB):" />
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>
							<TextBox x:Name="OutlineColorTextBox" Grid.Column="0" TextChanged="OnSettingChanged" />
							<Button Grid.Column="1" Content="Pick Color" Margin="5,0,0,0"
									Click="PickOutlineColorButton_Click" VerticalAlignment="Center" />
						</Grid>

						<CheckBox x:Name="ProcessEnabledCheckBox" Content="Only show in Target Process"
								  Margin="0,15,0,0" Checked="OnProcessCheckChanged" Unchecked="OnProcessCheckChanged" />

						<Label Content="Target Process Name (.exe):" />
						<TextBox x:Name="ProcessTextBox" TextChanged="OnProcessChanged" />
					</StackPanel>
				</GroupBox>

				<GroupBox Header="Dimensions">
					<StackPanel>
						<Label x:Name="LengthLabel" Content="Length (Horizontal):" />
						<Slider x:Name="LengthSlider" ValueChanged="OnSettingChanged" />
						<Label x:Name="LengthYLabel" Content="Length (Vertical):" />
						<Slider x:Name="LengthYSlider" ValueChanged="OnSettingChanged" />
						<Label x:Name="ThicknessLabel" Content="Thickness (Horizontal):" />
						<Slider x:Name="ThicknessSlider" ValueChanged="OnSettingChanged" />
						<Label x:Name="ThicknessYLabel" Content="Thickness (Vertical):" />
						<Slider x:Name="ThicknessYSlider" ValueChanged="OnSettingChanged" />
						<Label x:Name="GapLabel" Content="Gap:" />
						<Slider x:Name="GapSlider" ValueChanged="OnSettingChanged" />
					</StackPanel>
				</GroupBox>

				<GroupBox Header="Appearance">
					<StackPanel>
						<Label x:Name="OutlineThicknessLabel" Content="Outline Thickness:" />
						<Slider x:Name="OutlineThicknessSlider" ValueChanged="OnSettingChanged" />
						<Label x:Name="OpacityLabel" Content="Opacity:" />
						<Slider x:Name="OpacitySlider" ValueChanged="OnSettingChanged" />
						<Label x:Name="RotationLabel" Content="Rotation:" />
						<Slider x:Name="RotationSlider" ValueChanged="OnSettingChanged" />
					</StackPanel>
				</GroupBox>

				<GroupBox Header="Position">
					<StackPanel>
						<Label x:Name="XOffsetLabel" Content="X Offset:" />
						<Slider x:Name="XOffsetSlider" ValueChanged="OnSettingChanged" />
						<Label x:Name="YOffsetLabel" Content="Y Offset:" />
						<Slider x:Name="YOffsetSlider" ValueChanged="OnSettingChanged" />
					</StackPanel>
				</GroupBox>
			</StackPanel>
		</ScrollViewer>

		<Border Grid.Row="2" Background="{StaticResource FooterBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="20,10">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<TextBlock x:Name="versionLabel" Text="" Grid.Column="1"
                           Foreground="{StaticResource SecondaryForegroundBrush}" HorizontalAlignment="Right"
                           FontSize="11" />
				<Button x:Name="ToggleCrosshairButton" Background="{StaticResource SecondaryBackgroundBrush}" Content="Toggle Crosshair" Grid.Column="0" HorizontalAlignment="Left"
                        Click="ToggleCrosshair_Click" />
			</Grid>
		</Border>
	</Grid>
</Window>